const CustomError = require('../errors/CustomError');
const errorCodes = require('../errors/code');

const actionDao = require('../daos/user');

const findAll = async ({ id }) => {};

module.exports = { findAll };
